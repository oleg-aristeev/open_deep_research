"""Verification layer (epistemic layer) for the verified deep research fork.

This package implements the verify phase described in deep_research_mvp_plan.md:
claimify -> skeptic -> counter-evidence hunting -> stance -> aggregation ->
verified report composition with an attribution linter.

All code here is ours (not upstream ODR); patches to ODR files are marked
with ``# PATCH(verify)``.
"""
